<%= button_to 'Add Food', new_user_food_path, type: 'button', class: 'btn btn-primary' %>

<% if @foods.length > 0 %>
    <%= render partial: 'foods', locals: {foods: @foods } %>
<% else %>
    <%= 'No food to display' %>
<% end %>

